# HELD at 1.10.6. sbt 1.13.0 roughly doubles the engine native image (492 MB
# against a 100-300 MB gate) and widens Native Image reachability. See
# https://github.com/jdunkerley/enso/issues/34 — that issue also blocks
# sbt-scalafmt 2.6.x, which requires sbt 1.12.9+.
# Keep `sbtVersion` in .github/workflows/formatting.yml in sync with this.
sbt.version=1.10.6
