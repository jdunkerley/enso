# This file is auto-generated. Do not edit it manually!
# Edit the enso_build::ci_gen module instead and run `cargo run --package enso-build-ci-gen`.

name: IDE Packaging
on:
  workflow_dispatch:
    inputs:
      clean_build_required:
        description: Clean before and after the run.
        required: false
        type: boolean
        default: false
  workflow_call: {}
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-ide-packaging
  cancel-in-progress: ${{ github.ref != 'refs/heads/develop' }}
jobs:
  build-script-linux-amd64:
    name: Build Script
    runs-on:
      - ubuntu-latest
    steps:
      - name: Checking out the repository
        uses: actions/checkout@v4
        with:
          clean: false
      - name: Compile
        run: cargo build -p enso-build-cli
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          if-no-files-found: error
          name: enso-build-cli-${{ runner.os }}
          path: target/rust/debug/enso-build-cli
          retention-days: 1
  build-script-windows-amd64:
    name: Build Script
    runs-on:
      - windows-latest
    steps:
      - name: Checking out the repository
        uses: actions/checkout@v4
        with:
          clean: false
      - name: Compile
        run: cargo build -p enso-build-cli
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          if-no-files-found: error
          name: enso-build-cli-${{ runner.os }}
          path: target/rust/debug/enso-build-cli.exe
          retention-days: 1
  enso-build-ci-gen-job-build-backend-linux-amd64:
    name: Build Backend (linux, amd64)
    needs:
      - build-script-linux-amd64
    runs-on:
      - ubuntu-latest
    steps:
      - name: Expose Artifact API and context information.
        uses: actions/github-script@v7
        with:
          script: "\n    core.exportVariable(\"ACTIONS_RUNTIME_TOKEN\", process.env[\"ACTIONS_RUNTIME_TOKEN\"])\n    core.exportVariable(\"ACTIONS_RUNTIME_URL\", process.env[\"ACTIONS_RUNTIME_URL\"])\n    core.exportVariable(\"GITHUB_RETENTION_DAYS\", process.env[\"GITHUB_RETENTION_DAYS\"])\n    console.log(context)\n    "
      - name: Checking out the repository
        uses: actions/checkout@v4
        with:
          clean: false
      - if: runner.os == 'macOS'
        name: Setup nodejs version
        uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
      - if: runner.os != 'Linux'
        run: npm install -g --force corepack@0.36.0 && corepack --version
      - name: Download Build Script
        uses: actions/download-artifact@v4
        with:
          name: enso-build-cli-${{ runner.os }}
          path: ${{ runner.temp }}/enso-build-cli
      - name: Configure Build Script
        run: |-
          BIN="$RUNNER_TEMP/enso-build-cli/enso-build-cli"
          [ "$RUNNER_OS" = "Windows" ] && BIN="$BIN.exe"
          chmod +x "$BIN" || true
          echo "ENSO_BUILD_CLI_BIN=$BIN" >> "$GITHUB_ENV"
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Build Script Setup
        run: ./run --help
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - if: "${{ (contains(github.event.pull_request.labels.*.name, 'CI: Clean build required') || (github.ref == 'refs/heads/develop') || inputs.clean_build_required) }}"
        name: Clean before
        run: corepack pnpm run git-clean --verbose
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - run: ./run backend get
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Upload Edition File
        uses: actions/upload-artifact@v4
        with:
          name: Edition File
          path: distribution/editions/*.yaml
      - name: Upload fbs-schema
        uses: actions/upload-artifact@v4
        with:
          name: fbs-schema
          path: engine/language-server/src/main/schema/
      - name: Archive backend
        run: tar -cvf backend.tar -C dist/backend .
      - name: Upload backend
        uses: actions/upload-artifact@v4
        with:
          name: backend-linux
          path: backend.tar
      - name: Cleanup
        run: rm backend.tar
      - if: "${{ (always()) && (contains(github.event.pull_request.labels.*.name, 'CI: Clean build required') || (github.ref == 'refs/heads/develop') || inputs.clean_build_required) }}"
        name: Clean after
        run: corepack pnpm run git-clean --verbose
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    env:
      ENSO_LAUNCHER: native
  enso-build-ci-gen-job-build-backend-windows-amd64:
    name: Build Backend (windows, amd64)
    needs:
      - build-script-windows-amd64
    runs-on:
      - windows-latest
    steps:
      - name: Expose Artifact API and context information.
        uses: actions/github-script@v7
        with:
          script: "\n    core.exportVariable(\"ACTIONS_RUNTIME_TOKEN\", process.env[\"ACTIONS_RUNTIME_TOKEN\"])\n    core.exportVariable(\"ACTIONS_RUNTIME_URL\", process.env[\"ACTIONS_RUNTIME_URL\"])\n    core.exportVariable(\"GITHUB_RETENTION_DAYS\", process.env[\"GITHUB_RETENTION_DAYS\"])\n    console.log(context)\n    "
      - name: Checking out the repository
        uses: actions/checkout@v4
        with:
          clean: false
      - if: runner.os == 'macOS'
        name: Setup nodejs version
        uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
      - if: runner.os != 'Linux'
        run: npm install -g --force corepack@0.36.0 && corepack --version
      - name: Download Build Script
        uses: actions/download-artifact@v4
        with:
          name: enso-build-cli-${{ runner.os }}
          path: ${{ runner.temp }}/enso-build-cli
      - name: Configure Build Script
        run: |-
          BIN="$RUNNER_TEMP/enso-build-cli/enso-build-cli"
          [ "$RUNNER_OS" = "Windows" ] && BIN="$BIN.exe"
          chmod +x "$BIN" || true
          echo "ENSO_BUILD_CLI_BIN=$BIN" >> "$GITHUB_ENV"
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Build Script Setup
        run: ./run --help
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - if: "${{ (contains(github.event.pull_request.labels.*.name, 'CI: Clean build required') || (github.ref == 'refs/heads/develop') || inputs.clean_build_required) }}"
        name: Clean before
        run: corepack pnpm run git-clean --verbose
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - run: ./run backend get
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Archive backend
        run: tar -cvf backend.tar -C dist/backend .
      - name: Upload backend
        uses: actions/upload-artifact@v4
        with:
          name: backend-windows
          path: backend.tar
      - name: Cleanup
        run: rm backend.tar
      - if: "${{ (always()) && (contains(github.event.pull_request.labels.*.name, 'CI: Clean build required') || (github.ref == 'refs/heads/develop') || inputs.clean_build_required) }}"
        name: Clean after
        run: corepack pnpm run git-clean --verbose
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    env:
      ENSO_LAUNCHER: native
  enso-build-ci-gen-job-gui-build-linux-amd64:
    name: GUI build (linux, amd64)
    needs:
      - build-script-linux-amd64
    runs-on:
      - ubuntu-latest
    steps:
      - name: Expose Artifact API and context information.
        uses: actions/github-script@v7
        with:
          script: "\n    core.exportVariable(\"ACTIONS_RUNTIME_TOKEN\", process.env[\"ACTIONS_RUNTIME_TOKEN\"])\n    core.exportVariable(\"ACTIONS_RUNTIME_URL\", process.env[\"ACTIONS_RUNTIME_URL\"])\n    core.exportVariable(\"GITHUB_RETENTION_DAYS\", process.env[\"GITHUB_RETENTION_DAYS\"])\n    console.log(context)\n    "
      - name: Checking out the repository
        uses: actions/checkout@v4
        with:
          clean: false
      - if: runner.os == 'macOS'
        name: Setup nodejs version
        uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
      - if: runner.os != 'Linux'
        run: npm install -g --force corepack@0.36.0 && corepack --version
      - name: Download Build Script
        uses: actions/download-artifact@v4
        with:
          name: enso-build-cli-${{ runner.os }}
          path: ${{ runner.temp }}/enso-build-cli
      - name: Configure Build Script
        run: |-
          BIN="$RUNNER_TEMP/enso-build-cli/enso-build-cli"
          [ "$RUNNER_OS" = "Windows" ] && BIN="$BIN.exe"
          chmod +x "$BIN" || true
          echo "ENSO_BUILD_CLI_BIN=$BIN" >> "$GITHUB_ENV"
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Build Script Setup
        run: ./run --help
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - if: "${{ (contains(github.event.pull_request.labels.*.name, 'CI: Clean build required') || (github.ref == 'refs/heads/develop') || inputs.clean_build_required) }}"
        name: Clean before
        run: corepack pnpm run git-clean --verbose
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - run: ./run gui build
        env:
          ENSO_IDE_AG_GRID_LICENSE_KEY: ${{ vars.ENSO_AG_GRID_LICENSE_KEY }}
          ENSO_IDE_API_URL: ${{ vars.ENSO_CLOUD_API_URL }}
          ENSO_IDE_AUTH_ENDPOINT: ${{ vars.ENSO_CLOUD_AUTH_ENDPOINT }}
          ENSO_IDE_CHAT_URL: ${{ vars.ENSO_CLOUD_CHAT_URL }}
          ENSO_IDE_COGNITO_DOMAIN: ${{ vars.ENSO_CLOUD_COGNITO_DOMAIN }}
          ENSO_IDE_COGNITO_REGION: ${{ vars.ENSO_CLOUD_COGNITO_REGION }}
          ENSO_IDE_COGNITO_USER_POOL_ID: ${{ vars.ENSO_CLOUD_COGNITO_USER_POOL_ID }}
          ENSO_IDE_COGNITO_USER_POOL_WEB_CLIENT_ID: ${{ vars.ENSO_CLOUD_COGNITO_USER_POOL_WEB_CLIENT_ID }}
          ENSO_IDE_ENVIRONMENT: ${{ vars.ENSO_CLOUD_ENVIRONMENT }}
          ENSO_IDE_GOOGLE_ANALYTICS_TAG: ${{ vars.ENSO_CLOUD_GOOGLE_ANALYTICS_TAG }}
          ENSO_IDE_GOOGLE_OAUTH_CLIENT_ID: ${{ secrets.ENSO_IDE_GOOGLE_OAUTH_CLIENT_ID }}
          ENSO_IDE_HOST: ${{ vars.ENSO_HOST }}
          ENSO_IDE_MAPBOX_API_TOKEN: ${{ vars.ENSO_MAPBOX_API_TOKEN }}
          ENSO_IDE_MS365_OAUTH_CLIENT_ID: ${{ secrets.ENSO_IDE_MS365_OAUTH_CLIENT_ID }}
          ENSO_IDE_SALESFORCE_OAUTH_CLIENT_ID: ${{ secrets.ENSO_IDE_SALESFORCE_OAUTH_CLIENT_ID }}
          ENSO_IDE_SENTRY_DSN: ${{ vars.ENSO_CLOUD_SENTRY_DSN }}
          ENSO_IDE_STRAVA_OAUTH_CLIENT_ID: ${{ secrets.ENSO_IDE_STRAVA_OAUTH_CLIENT_ID }}
          ENSO_IDE_STRIPE_KEY: ${{ vars.ENSO_CLOUD_STRIPE_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Upload gui
        uses: actions/upload-artifact@v4
        with:
          name: gui
          path: dist/gui/
      - if: "${{ (always()) && (contains(github.event.pull_request.labels.*.name, 'CI: Clean build required') || (github.ref == 'refs/heads/develop') || inputs.clean_build_required) }}"
        name: Clean after
        run: corepack pnpm run git-clean --verbose
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  enso-build-ci-gen-job-gui-build-windows-amd64:
    name: GUI build (windows, amd64)
    needs:
      - build-script-windows-amd64
    runs-on:
      - windows-latest
    steps:
      - name: Expose Artifact API and context information.
        uses: actions/github-script@v7
        with:
          script: "\n    core.exportVariable(\"ACTIONS_RUNTIME_TOKEN\", process.env[\"ACTIONS_RUNTIME_TOKEN\"])\n    core.exportVariable(\"ACTIONS_RUNTIME_URL\", process.env[\"ACTIONS_RUNTIME_URL\"])\n    core.exportVariable(\"GITHUB_RETENTION_DAYS\", process.env[\"GITHUB_RETENTION_DAYS\"])\n    console.log(context)\n    "
      - name: Checking out the repository
        uses: actions/checkout@v4
        with:
          clean: false
      - if: runner.os == 'macOS'
        name: Setup nodejs version
        uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
      - if: runner.os != 'Linux'
        run: npm install -g --force corepack@0.36.0 && corepack --version
      - name: Download Build Script
        uses: actions/download-artifact@v4
        with:
          name: enso-build-cli-${{ runner.os }}
          path: ${{ runner.temp }}/enso-build-cli
      - name: Configure Build Script
        run: |-
          BIN="$RUNNER_TEMP/enso-build-cli/enso-build-cli"
          [ "$RUNNER_OS" = "Windows" ] && BIN="$BIN.exe"
          chmod +x "$BIN" || true
          echo "ENSO_BUILD_CLI_BIN=$BIN" >> "$GITHUB_ENV"
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Build Script Setup
        run: ./run --help
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - if: "${{ (contains(github.event.pull_request.labels.*.name, 'CI: Clean build required') || (github.ref == 'refs/heads/develop') || inputs.clean_build_required) }}"
        name: Clean before
        run: corepack pnpm run git-clean --verbose
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - run: ./run gui build
        env:
          ENSO_IDE_AG_GRID_LICENSE_KEY: ${{ vars.ENSO_AG_GRID_LICENSE_KEY }}
          ENSO_IDE_API_URL: ${{ vars.ENSO_CLOUD_API_URL }}
          ENSO_IDE_AUTH_ENDPOINT: ${{ vars.ENSO_CLOUD_AUTH_ENDPOINT }}
          ENSO_IDE_CHAT_URL: ${{ vars.ENSO_CLOUD_CHAT_URL }}
          ENSO_IDE_COGNITO_DOMAIN: ${{ vars.ENSO_CLOUD_COGNITO_DOMAIN }}
          ENSO_IDE_COGNITO_REGION: ${{ vars.ENSO_CLOUD_COGNITO_REGION }}
          ENSO_IDE_COGNITO_USER_POOL_ID: ${{ vars.ENSO_CLOUD_COGNITO_USER_POOL_ID }}
          ENSO_IDE_COGNITO_USER_POOL_WEB_CLIENT_ID: ${{ vars.ENSO_CLOUD_COGNITO_USER_POOL_WEB_CLIENT_ID }}
          ENSO_IDE_ENVIRONMENT: ${{ vars.ENSO_CLOUD_ENVIRONMENT }}
          ENSO_IDE_GOOGLE_ANALYTICS_TAG: ${{ vars.ENSO_CLOUD_GOOGLE_ANALYTICS_TAG }}
          ENSO_IDE_GOOGLE_OAUTH_CLIENT_ID: ${{ secrets.ENSO_IDE_GOOGLE_OAUTH_CLIENT_ID }}
          ENSO_IDE_HOST: ${{ vars.ENSO_HOST }}
          ENSO_IDE_MAPBOX_API_TOKEN: ${{ vars.ENSO_MAPBOX_API_TOKEN }}
          ENSO_IDE_MS365_OAUTH_CLIENT_ID: ${{ secrets.ENSO_IDE_MS365_OAUTH_CLIENT_ID }}
          ENSO_IDE_SALESFORCE_OAUTH_CLIENT_ID: ${{ secrets.ENSO_IDE_SALESFORCE_OAUTH_CLIENT_ID }}
          ENSO_IDE_SENTRY_DSN: ${{ vars.ENSO_CLOUD_SENTRY_DSN }}
          ENSO_IDE_STRAVA_OAUTH_CLIENT_ID: ${{ secrets.ENSO_IDE_STRAVA_OAUTH_CLIENT_ID }}
          ENSO_IDE_STRIPE_KEY: ${{ vars.ENSO_CLOUD_STRIPE_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - if: "${{ (always()) && (contains(github.event.pull_request.labels.*.name, 'CI: Clean build required') || (github.ref == 'refs/heads/develop') || inputs.clean_build_required) }}"
        name: Clean after
        run: corepack pnpm run git-clean --verbose
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  enso-build-ci-gen-job-package-ide-linux-amd64:
    name: Package New IDE (linux, amd64)
    needs:
      - build-script-linux-amd64
      - enso-build-ci-gen-job-build-backend-linux-amd64
    runs-on:
      - ubuntu-latest
    steps:
      - name: Expose Artifact API and context information.
        uses: actions/github-script@v7
        with:
          script: "\n    core.exportVariable(\"ACTIONS_RUNTIME_TOKEN\", process.env[\"ACTIONS_RUNTIME_TOKEN\"])\n    core.exportVariable(\"ACTIONS_RUNTIME_URL\", process.env[\"ACTIONS_RUNTIME_URL\"])\n    core.exportVariable(\"GITHUB_RETENTION_DAYS\", process.env[\"GITHUB_RETENTION_DAYS\"])\n    console.log(context)\n    "
      - name: Checking out the repository
        uses: actions/checkout@v4
        with:
          clean: false
          fetch-depth: 2
      - if: runner.os == 'macOS'
        name: Setup nodejs version
        uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
      - if: runner.os != 'Linux'
        run: npm install -g --force corepack@0.36.0 && corepack --version
      - name: Download Build Script
        uses: actions/download-artifact@v4
        with:
          name: enso-build-cli-${{ runner.os }}
          path: ${{ runner.temp }}/enso-build-cli
      - name: Configure Build Script
        run: |-
          BIN="$RUNNER_TEMP/enso-build-cli/enso-build-cli"
          [ "$RUNNER_OS" = "Windows" ] && BIN="$BIN.exe"
          chmod +x "$BIN" || true
          echo "ENSO_BUILD_CLI_BIN=$BIN" >> "$GITHUB_ENV"
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Build Script Setup
        run: ./run --help
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - if: "${{ (contains(github.event.pull_request.labels.*.name, 'CI: Clean build required') || (github.ref == 'refs/heads/develop') || inputs.clean_build_required) }}"
        name: Clean before
        run: corepack pnpm run git-clean --verbose
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Download backend
        uses: actions/download-artifact@v4
        with:
          name: backend-linux
          path: dist/backend
      - run: |-
          tar -xvf dist/backend/backend.tar -C dist/backend
          rm dist/backend/backend.tar
      - run: ./run ide build --backend-source local --gui-upload-artifact false
        env:
          ENSO_IDE_AG_GRID_LICENSE_KEY: ${{ vars.ENSO_AG_GRID_LICENSE_KEY }}
          ENSO_IDE_API_URL: ${{ vars.ENSO_CLOUD_API_URL }}
          ENSO_IDE_AUTH_ENDPOINT: ${{ vars.ENSO_CLOUD_AUTH_ENDPOINT }}
          ENSO_IDE_CHAT_URL: ${{ vars.ENSO_CLOUD_CHAT_URL }}
          ENSO_IDE_COGNITO_DOMAIN: ${{ vars.ENSO_CLOUD_COGNITO_DOMAIN }}
          ENSO_IDE_COGNITO_REGION: ${{ vars.ENSO_CLOUD_COGNITO_REGION }}
          ENSO_IDE_COGNITO_USER_POOL_ID: ${{ vars.ENSO_CLOUD_COGNITO_USER_POOL_ID }}
          ENSO_IDE_COGNITO_USER_POOL_WEB_CLIENT_ID: ${{ vars.ENSO_CLOUD_COGNITO_USER_POOL_WEB_CLIENT_ID }}
          ENSO_IDE_ENVIRONMENT: ${{ vars.ENSO_CLOUD_ENVIRONMENT }}
          ENSO_IDE_GOOGLE_ANALYTICS_TAG: ${{ vars.ENSO_CLOUD_GOOGLE_ANALYTICS_TAG }}
          ENSO_IDE_GOOGLE_OAUTH_CLIENT_ID: ${{ secrets.ENSO_IDE_GOOGLE_OAUTH_CLIENT_ID }}
          ENSO_IDE_HOST: ${{ vars.ENSO_HOST }}
          ENSO_IDE_MAPBOX_API_TOKEN: ${{ vars.ENSO_MAPBOX_API_TOKEN }}
          ENSO_IDE_MS365_OAUTH_CLIENT_ID: ${{ secrets.ENSO_IDE_MS365_OAUTH_CLIENT_ID }}
          ENSO_IDE_SALESFORCE_OAUTH_CLIENT_ID: ${{ secrets.ENSO_IDE_SALESFORCE_OAUTH_CLIENT_ID }}
          ENSO_IDE_SENTRY_DSN: ${{ vars.ENSO_CLOUD_SENTRY_DSN }}
          ENSO_IDE_STRAVA_OAUTH_CLIENT_ID: ${{ secrets.ENSO_IDE_STRAVA_OAUTH_CLIENT_ID }}
          ENSO_IDE_STRIPE_KEY: ${{ vars.ENSO_CLOUD_STRIPE_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Upload ide
        uses: actions/upload-artifact@v4
        with:
          name: ide-linux
          path: dist/ide/enso-*.AppImage
      - name: Prepare Package Tests
        run: 'mkdir -p app/electron-client/playwright/.auth && touch app/electron-client/playwright/.auth/user.json && chmod 600 app/electron-client/playwright/.auth/user.json && echo "{\"user\": \"$ENSO_TEST_USER\",\"password\":\"$ENSO_TEST_USER_PASSWORD\"}" > app/electron-client/playwright/.auth/user.json'
        shell: bash
        env:
          ENSO_TEST_USER: ${{ secrets.ENSO_CLOUD_TEST_ACCOUNT_USERNAME }}
          ENSO_TEST_USER_PASSWORD: ${{ secrets.ENSO_CLOUD_TEST_ACCOUNT_PASS }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Run Package Tests
        run: xvfb-run corepack pnpm -r --filter enso ide-integration-test
        env:
          DEBUG: "pw:browser log:"
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - if: failure()
        name: Upload Test Traces
        uses: actions/upload-artifact@v4
        with:
          compression-level: 0
          name: test-traces-linux-amd64
          path: app/electron-client/test-traces
      - if: always()
        name: Remove Credentials File
        run: rm -f $HOME/.enso/credentials
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - if: "${{ (always()) && (contains(github.event.pull_request.labels.*.name, 'CI: Clean build required') || (github.ref == 'refs/heads/develop') || inputs.clean_build_required) }}"
        name: Clean after
        run: corepack pnpm run git-clean --verbose
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  enso-build-ci-gen-job-package-ide-windows-amd64:
    name: Package New IDE (windows, amd64)
    needs:
      - build-script-windows-amd64
      - enso-build-ci-gen-job-build-backend-windows-amd64
    runs-on:
      - windows-latest
    steps:
      - name: Expose Artifact API and context information.
        uses: actions/github-script@v7
        with:
          script: "\n    core.exportVariable(\"ACTIONS_RUNTIME_TOKEN\", process.env[\"ACTIONS_RUNTIME_TOKEN\"])\n    core.exportVariable(\"ACTIONS_RUNTIME_URL\", process.env[\"ACTIONS_RUNTIME_URL\"])\n    core.exportVariable(\"GITHUB_RETENTION_DAYS\", process.env[\"GITHUB_RETENTION_DAYS\"])\n    console.log(context)\n    "
      - name: Checking out the repository
        uses: actions/checkout@v4
        with:
          clean: false
          fetch-depth: 2
      - if: runner.os == 'macOS'
        name: Setup nodejs version
        uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
      - if: runner.os != 'Linux'
        run: npm install -g --force corepack@0.36.0 && corepack --version
      - name: Download Build Script
        uses: actions/download-artifact@v4
        with:
          name: enso-build-cli-${{ runner.os }}
          path: ${{ runner.temp }}/enso-build-cli
      - name: Configure Build Script
        run: |-
          BIN="$RUNNER_TEMP/enso-build-cli/enso-build-cli"
          [ "$RUNNER_OS" = "Windows" ] && BIN="$BIN.exe"
          chmod +x "$BIN" || true
          echo "ENSO_BUILD_CLI_BIN=$BIN" >> "$GITHUB_ENV"
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Build Script Setup
        run: ./run --help
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - if: "${{ (contains(github.event.pull_request.labels.*.name, 'CI: Clean build required') || (github.ref == 'refs/heads/develop') || inputs.clean_build_required) }}"
        name: Clean before
        run: corepack pnpm run git-clean --verbose
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Download backend
        uses: actions/download-artifact@v4
        with:
          name: backend-windows
          path: dist/backend
      - run: |-
          tar -xvf dist/backend/backend.tar -C dist/backend
          rm dist/backend/backend.tar
      - run: ./run ide build --backend-source local --gui-upload-artifact false
        env:
          ENSO_IDE_AG_GRID_LICENSE_KEY: ${{ vars.ENSO_AG_GRID_LICENSE_KEY }}
          ENSO_IDE_API_URL: ${{ vars.ENSO_CLOUD_API_URL }}
          ENSO_IDE_AUTH_ENDPOINT: ${{ vars.ENSO_CLOUD_AUTH_ENDPOINT }}
          ENSO_IDE_CHAT_URL: ${{ vars.ENSO_CLOUD_CHAT_URL }}
          ENSO_IDE_COGNITO_DOMAIN: ${{ vars.ENSO_CLOUD_COGNITO_DOMAIN }}
          ENSO_IDE_COGNITO_REGION: ${{ vars.ENSO_CLOUD_COGNITO_REGION }}
          ENSO_IDE_COGNITO_USER_POOL_ID: ${{ vars.ENSO_CLOUD_COGNITO_USER_POOL_ID }}
          ENSO_IDE_COGNITO_USER_POOL_WEB_CLIENT_ID: ${{ vars.ENSO_CLOUD_COGNITO_USER_POOL_WEB_CLIENT_ID }}
          ENSO_IDE_ENVIRONMENT: ${{ vars.ENSO_CLOUD_ENVIRONMENT }}
          ENSO_IDE_GOOGLE_ANALYTICS_TAG: ${{ vars.ENSO_CLOUD_GOOGLE_ANALYTICS_TAG }}
          ENSO_IDE_GOOGLE_OAUTH_CLIENT_ID: ${{ secrets.ENSO_IDE_GOOGLE_OAUTH_CLIENT_ID }}
          ENSO_IDE_HOST: ${{ vars.ENSO_HOST }}
          ENSO_IDE_MAPBOX_API_TOKEN: ${{ vars.ENSO_MAPBOX_API_TOKEN }}
          ENSO_IDE_MS365_OAUTH_CLIENT_ID: ${{ secrets.ENSO_IDE_MS365_OAUTH_CLIENT_ID }}
          ENSO_IDE_SALESFORCE_OAUTH_CLIENT_ID: ${{ secrets.ENSO_IDE_SALESFORCE_OAUTH_CLIENT_ID }}
          ENSO_IDE_SENTRY_DSN: ${{ vars.ENSO_CLOUD_SENTRY_DSN }}
          ENSO_IDE_STRAVA_OAUTH_CLIENT_ID: ${{ secrets.ENSO_IDE_STRAVA_OAUTH_CLIENT_ID }}
          ENSO_IDE_STRIPE_KEY: ${{ vars.ENSO_CLOUD_STRIPE_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          WIN_CSC_KEY_PASSWORD: ${{ secrets.MICROSOFT_CODE_SIGNING_CERT_PASSWORD }}
          WIN_CSC_LINK: ${{ secrets.MICROSOFT_CODE_SIGNING_CERT }}
      - name: Upload ide
        uses: actions/upload-artifact@v4
        with:
          name: ide-windows
          path: dist/ide/enso-*.exe
      - name: Prepare Package Tests
        run: 'mkdir -p app/electron-client/playwright/.auth && touch app/electron-client/playwright/.auth/user.json && chmod 600 app/electron-client/playwright/.auth/user.json && echo "{\"user\": \"$ENSO_TEST_USER\",\"password\":\"$ENSO_TEST_USER_PASSWORD\"}" > app/electron-client/playwright/.auth/user.json'
        shell: bash
        env:
          ENSO_TEST_USER: ${{ secrets.ENSO_CLOUD_TEST_ACCOUNT_USERNAME }}
          ENSO_TEST_USER_PASSWORD: ${{ secrets.ENSO_CLOUD_TEST_ACCOUNT_PASS }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Run Package Tests
        run: corepack pnpm -r --filter enso ide-integration-test
        env:
          DEBUG: "pw:browser log:"
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - if: failure()
        name: Upload Test Traces
        uses: actions/upload-artifact@v4
        with:
          compression-level: 0
          name: test-traces-windows-amd64
          path: app/electron-client/test-traces
      - if: always()
        name: Remove Credentials File
        run: rm -f $HOME/.enso/credentials
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - if: "${{ (always()) && (contains(github.event.pull_request.labels.*.name, 'CI: Clean build required') || (github.ref == 'refs/heads/develop') || inputs.clean_build_required) }}"
        name: Clean after
        run: corepack pnpm run git-clean --verbose
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
env:
  ENSO_BUILD_SKIP_VERSION_CHECK: "true"
